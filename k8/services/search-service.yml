apiVersion: v1
kind: Service
metadata:
    name: server-service
    namespace: team-promptheus
spec:
    selector:
        app: server
    ports:
        - protocol: TCP
          port: 8070
          targetPort: 8070
